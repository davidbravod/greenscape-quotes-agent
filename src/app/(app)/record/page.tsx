export default function RecordPage() {
  return (
    <div className="space-y-4">
      <h1 className="text-xl font-semibold">New site-walk recording</h1>
      <p className="text-sm text-black/60">
        Recorder UI (MediaRecorder + upload) — coming next.
      </p>
    </div>
  );
}
